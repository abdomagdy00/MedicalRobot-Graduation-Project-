

namespace Application.DTOs
{
    namespace Application.DTOs
    {
        public record LoginDto(string FullName, string CollegeId);
        public record RegisterDto(string FullName, string CollegeId, string Email);
    }
}
